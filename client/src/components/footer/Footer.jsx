import React from "react";
import styles from "./footer.module.css";

const Footer = props => (
	<footer className={styles.footer}>
		<p className={styles.title}>made by YGM(a.k.a. yeogi gosu maniyo.)</p>
	</footer>
);

export default Footer;
